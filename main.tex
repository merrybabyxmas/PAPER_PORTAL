\documentclass{article}

\usepackage{neurips_2026}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{pifont}
\usepackage{caption}
\usepackage{bbm}

% Theorem environments
\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}{Remark}

% Custom commands
\newcommand{\Wbase}{W_0}
\newcommand{\Aseq}{A_{\mathrm{s}}}
\newcommand{\Bseq}{B_{\mathrm{s}}}
\newcommand{\Apar}{A_{\mathrm{p}}}
\newcommand{\Bpar}{B_{\mathrm{p}}}
\newcommand{\Tswitch}{T_{\mathrm{sw}}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\jelly}{\textsc{Jelly}}
\DeclareMathOperator{\rank}{rank}
\DeclareMathOperator{\diag}{diag}
\DeclareMathOperator{\tr}{tr}

\title{\jelly{}: Generalizing Parameter-Efficient Fine-Tuning\\over the Time Axis via Task-Isomorphic Projection}

\author{Anonymous Author(s)}

\begin{document}

\maketitle

%=============================================================================
\begin{abstract}
%=============================================================================
Parameter-efficient fine-tuning (PEFT) methods such as LoRA fix the adapter topology to a \emph{parallel} connection, bypassing the pretrained weight manifold ($\Wbase$) that sequential adapters exploit for task adaptation.
SVD-based initialization methods (e.g., PiSSA) attempt to utilize $\Wbase$ but require $O(d^3)$ decomposition and remain task-agnostic.
We introduce \jelly{}, a PEFT framework that unifies sequential and parallel adaptations along the training time axis.
During an initial sequential phase, \jelly{} projects activations through $\Wbase$ ($\Wbase x$) to identify the task-intrinsic subspace from data.
At a switching step $\Tswitch$, a lossless \emph{Task-Isomorphic Projection} converts the learned sequential representations into an equivalent parallel adapter via a lightweight double SVD.
This costs $O(dr^2)$---orders of magnitude below full-weight SVD---while preserving the zero additional latency property of LoRA at inference.
Standard LoRA is recovered as the special case $\Tswitch = 0$.
Experiments on LLaMA-2-7B and DeBERTa-v3-base across mathematical reasoning (GSM8K, MATH), code generation (HumanEval, MBPP), conversation (MT-Bench), and seven GLUE tasks show that \jelly{} consistently outperforms LoRA, PiSSA, and DoRA, achieving the best score on 6 out of 7 NLU tasks and the highest MATH accuracy.
We also show that $\Tswitch$ serves as a geometric indicator of the alignment between the target task and the pretrained manifold.
\end{abstract}
%=============================================================================
\section{Introduction}
\label{sec:intro}
%=============================================================================

Large language models (LLMs) with billions of parameters~\citep{brown2020gpt3, touvron2023llama2} have become the \emph{de facto} foundation for natural language processing and reasoning tasks.
Full fine-tuning of these models is expensive in compute and memory, motivating parameter-efficient fine-tuning (PEFT) methods that update only a small fraction of parameters while keeping the pretrained weights frozen~\citep{lialin2023scaling}.

Among PEFT methods, Low-Rank Adaptation (LoRA)~\citep{hu2022lora} has emerged as the dominant paradigm.
LoRA injects trainable low-rank matrices $B \in \mathbb{R}^{d \times r}$ and $A \in \mathbb{R}^{r \times d}$ in \emph{parallel} with each frozen weight $\Wbase$, yielding the forward pass $h = \Wbase x + BAx$.\footnote{For notational simplicity in the introduction, we assume $d_{\mathrm{in}} = d_{\mathrm{out}} = d$. The general case is treated in Section~\ref{sec:method}.}
The adapter can be reparameterized into $\Wbase$ after training, guaranteeing \emph{zero additional inference latency}.
However, LoRA's random initialization means the early optimization steps operate in a subspace disconnected from the pretrained representations of $\Wbase$, leading to slow initial convergence.

To mitigate this, PiSSA~\citep{meng2024pissa} initializes $B$ and $A$ using the principal singular vectors of $\Wbase$.
While SVD-based initialization accelerates convergence, it requires a full SVD over every target weight matrix at $O(d^3)$ cost per layer.
Because this decomposition is performed before observing any data, it captures the structure of $\Wbase$ but remains \emph{task-agnostic}.

The earliest PEFT methods employed a \emph{sequential} topology~\citep{houlsby2019parameter}, where the adapter processes the output of the base layer: $h = \Wbase x + BA(\Wbase x)$.
This structure uses the feature space of $\Wbase$ as an implicit prior, projecting activations through the pretrained manifold before adaptation.
Sequential adapters were abandoned in favor of parallel methods because they incur inference latency proportional to the model's depth.

We challenge the assumption that sequential and parallel topologies must be mutually exclusive.
We introduce \jelly{} (\textbf{J}oint \textbf{E}fficient \textbf{L}earning with \textbf{L}ayer-\textbf{Y}ielding), a framework that \emph{generalizes} adapter connectivity over the training time axis.
\jelly{} combines the optimization benefits of sequential probing with the inference efficiency of parallel adaptation. Our contributions:

\begin{itemize}
    \item[\bf C1.] \textbf{Time-Axis Generalization.} We formulate adapter connectivity as a function of training progress, transitioning from sequential to parallel at a switching point $\Tswitch \in [0, 1]$. LoRA is recovered as $\Tswitch = 0$.
    \item[\bf C2.] \textbf{Task-Aware Probing.} The sequential phase probes the pretrained manifold from data, identifying the task-intrinsic subspace before parallel training begins.
    \item[\bf C3.] \textbf{$O(dr^2)$ Projection.} A rank-$r$ double SVD converts sequential adapters into equivalent parallel adapters, reducing PiSSA's $O(d^3)$ initialization cost by up to $10^6\times$.
    \item[\bf C4.] \textbf{Zero Inference Overhead.} After projection, training continues in standard LoRA topology with no additional latency at inference.
\end{itemize}

\begin{figure*}[t]
\centering
% Top row: (a) left | (b) right
\begin{subfigure}[b]{0.37\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/fig1a.png}
    \caption{Parallel (LoRA)}
    \label{fig:arch_parallel}
\end{subfigure}
\hfill
\vrule width 0.4pt
\hfill
\begin{subfigure}[b]{0.57\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/fig1b.png}
    \caption{Sequential (Houlsby)}
    \label{fig:arch_sequential}
\end{subfigure}
\par\vspace{0.3em}
\noindent\rule{\textwidth}{0.4pt}
\vspace{0.3em}
% Bottom row: (c) full width
\begin{subfigure}[b]{\textwidth}
    \centering
    \includegraphics[width=\textwidth]{figures/fig1c.png}
    \caption{\jelly{} (Ours)}
    \label{fig:arch_portal}
\end{subfigure}
\caption{\textbf{(a)} Parallel composition (LoRA): adapters are unaligned with $\Wbase$, leading to slow early convergence.
\textbf{(b)} Sequential composition (Houlsby): probes the base manifold but adds inference latency.
\textbf{(c)} \jelly{}: unifies both topologies over the training time axis ($t \in [0, 1]$). Phase~1 ($t < \Tswitch$) probes through $\Wbase$; Phase~2 ($t \ge \Tswitch$) operates in parallel after a lossless $O(dr^2)$ Task-Isomorphic Projection, with zero added inference latency. LoRA is the special case $\Tswitch\!=\!0$.}
\label{fig:architecture}
\end{figure*}

%=============================================================================
\section{Related Work}
\label{sec:related}
%=============================================================================

\paragraph{Parallel Low-Rank Adaptation and Optimization.}
LoRA~\citep{hu2022lora} injects trainable low-rank matrices in a \emph{parallel} topology to ensure zero additional inference latency.
Subsequent work optimizes training dynamics and representational capacity:
DoRA~\citep{liu2024dora} decomposes weight updates into magnitude and direction components;
LoRA+~\citep{hayou2024lora+} assigns distinct learning rates for $A$ and $B$ to address gradient imbalance;
AdaLoRA~\citep{zhang2023adalora} distributes rank budgets based on singular value importance; and
DyLoRA~\citep{valipour2022dylora} trains across multiple ranks simultaneously.
All of these constrain the adapter to a \emph{static} parallel topology, bypassing the pretrained manifold during early optimization.

\paragraph{The SVD Initialization Bottleneck.}
PiSSA~\citep{meng2024pissa} initializes parallel adapters using the principal singular components of $\Wbase$, accelerating convergence but requiring $O(d^3)$ full SVD per layer.
Because this decomposition occurs before observing any data, it remains \emph{task-agnostic}.
\jelly{} delays decomposition to the switching point $\Tswitch$, requiring only an $O(dr^2)$ double SVD on a learned, \emph{task-aware} matrix.

\paragraph{Sequential Topologies and Time-Axis Generalization.}
Houlsby adapters~\citep{houlsby2019parameter} use a \emph{sequential} topology, inserting bottleneck layers after pretrained layers so the adapter operates on $\Wbase x$. AdapterFusion~\citep{pfeiffer2020adapterfusion} combines multiple such adapters via attention.
Sequential adapters incur inference latency proportional to model depth, which led to their abandonment.
\jelly{} resolves this by generalizing the topology over the training time axis: a lossless \emph{Task-Isomorphic Projection} transfers the sequential adapter's manifold-aligned representations into the zero-latency parallel structure.

\paragraph{Intrinsic Dimensionality and Broad PEFT Paradigms.}
The intrinsic dimensionality perspective~\citep{aghajanyan2021intrinsic} shows that over-parameterized models can be fine-tuned within a low-dimensional subspace.
BitFit~\citep{zaken2022bitfit} optimizes only bias terms, Delta-LoRA~\citep{zi2023delta} propagates adapter deltas back to the base weights, and QLoRA~\citep{dettmers2023qlora} enables LoRA on quantized models.
\jelly{} is compatible with these paradigms; its activation-based probing and lightweight projection work directly with quantized base models.

%=============================================================================
\section{Methodology}
\label{sec:method}
%=============================================================================

\subsection{Preliminaries}

Consider a pretrained linear layer $\Wbase \in \mathbb{R}^{d_{\mathrm{out}} \times d_{\mathrm{in}}}$ and a low-rank adapter $(B \in \mathbb{R}^{d_{\mathrm{out}} \times r},\; A \in \mathbb{R}^{r \times d_{\mathrm{in}}})$ with $r \ll \min(d_{\mathrm{out}}, d_{\mathrm{in}})$.
PEFT methods differ in how the adapter receives its input.
\textbf{Parallel} adapters (LoRA) compute $h = \Wbase x + BAx$; \textbf{sequential} adapters (Houlsby) compute $h = \Wbase x + BA(\Wbase x)$.

\begin{table}[h]
\caption{PEFT adapter topologies, initialization, and gradient dynamics.}
\label{tab:topology}
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{lcccc}
\toprule
& \textbf{LoRA (Parallel)} & \textbf{Houlsby (Sequential)} & \textbf{PiSSA (Parallel)} & \textbf{\jelly{} (Ours)} \\
\midrule
Forward Pass          & $\Wbase x + BAx$           & $\Wbase x + BA(\Wbase x)$ & $\Wbase x + BAx$ & $\Wbase x + BA(\Wbase x) \to \Wbase x + BAx$ \\
Adapter Input         & $x$             & $\Wbase x$  & $x$ & $\Wbase x \to x$ \\
Effective $\Delta W$  & $BA$                   & $BA\Wbase$ & $BA$ & $BA\Wbase \to BA$ \\
$\nabla_A \mathcal{L}$ & $B^\top \delta \, x^\top$ & $B^\top \delta \, (\Wbase x)^\top$ & $B^\top \delta \, x^\top$ & $B^\top \delta \, (\Wbase x)^\top \to B^\top \delta \, x^\top$ \\
\midrule
SVD Target      & N/A & N/A & $\Wbase \in \mathbb{R}^{d \times d}$ & Core $\in \mathbb{R}^{r \times r}$ \\
Init.\ Cost    & $O(1)$         & $O(1)$ & $O(d^3)$ & $O(dr^2)$ \\
Task-Aware         & \ding{55}             & \ding{55}    & \ding{55}  & \ding{51} \\
Infer.\ Overhead     & None           & $O(dr)$/layer  & None & None \\
\bottomrule
\end{tabular}}
\end{table}

Let $\delta \triangleq \partial \mathcal{L}/\partial h$.
The key difference is in gradient structure: the sequential gradient $\nabla_A \mathcal{L} = B^\top \delta\, (\Wbase x)^\top$ biases early updates toward the principal directions of $\Wbase$, while the parallel gradient $\nabla_A \mathcal{L} = B^\top \delta\, x^\top$ treats all input directions uniformly (Table~\ref{tab:topology}).
PiSSA compensates via SVD-based initialization but at $O(d^3)$ cost per layer and without task awareness.
\jelly{} combines the gradient advantage of sequential probing with the inference efficiency of parallel adapters, using only an $O(dr^2)$ projection at the switching point.

\subsection{\jelly{}: Time-Axis Generalization}
\label{sec:portal}

Let $t \in [0, 1]$ denote the normalized training epoch. \jelly{} operates in sequential mode for $t < \Tswitch$ and parallel mode for $t \geq \Tswitch$:

\begin{equation}
h(t) = \Wbase x + B(t) A(t) \cdot
\begin{cases}
\Wbase x & \text{if } t < \Tswitch \quad (\text{Sequential Phase})\\
x & \text{if } t \geq \Tswitch \quad (\text{Parallel Phase})
\end{cases}
\label{eq:portal}
\end{equation}

Setting $\Tswitch = 0$ recovers standard LoRA; $\Tswitch = 1$ yields a pure sequential adapter.
We denote weights during the sequential phase as $\Bseq, \Aseq$ and the parallel phase as $\Bpar, \Apar$.


\subsection{Task-Isomorphic Projection via Rank-Bounded SVD}
\label{sec:projection}

At $t = \Tswitch$, we convert the sequential adapter $(\Bseq, \Aseq)$ into a parallel adapter $(\Bpar, \Apar)$ that preserves the forward output for any input $x$:
\begin{equation}
\Bpar \Apar x = \Bseq \Aseq (\Wbase x) \quad \Longrightarrow \quad \Bpar \Apar = \Bseq \Aseq \Wbase \triangleq M
\label{eq:equivalence}
\end{equation}

\paragraph{Rank bound.}
Since $M = \Bseq \Aseq \Wbase$ with $\Bseq \in \mathbb{R}^{d_{\mathrm{out}} \times r}$, the rank of $M$ is bounded:
\begin{equation*}
    \mathrm{rank}(M) \leq r
\end{equation*}

This bound enables an $O(dr^2)$ factorization instead of the $O(d^3)$ full-weight SVD:


\begin{itemize}
    \item \textbf{Step 1 (Double QR).} Extract orthogonal bases via thin QR on the sequential components:
    \begin{equation}
    \Bseq = Q_B R_B, \quad (\Aseq \Wbase)^\top = Q_X R_X
    \end{equation}
    
    \item \textbf{Step 2 (Core SVD).} Decompose the $r \times r$ core matrix $R_B R_X^\top$ and reconstruct the singular vectors of $M$:
    \begin{align}
    R_B R_X^\top &= \widetilde{U} \Sigma \widetilde{V}^\top \quad \text{(Core SVD)} \\
    M &= \underbrace{(Q_B \widetilde{U})}_{U} \Sigma \underbrace{(\widetilde{V}^\top Q_X^\top)}_{V^\top} \quad \text{(Reconstruction)}
    \end{align}
    
    \item \textbf{Step 3 (Symmetric Split).} Distribute singular values equally between the two adapters:
    \begin{equation}
    \Bpar = U \Sigma^{1/2}, \quad \Apar = \Sigma^{1/2} V^\top
    \end{equation}
\end{itemize}

\paragraph{Why symmetric splitting.}
An asymmetric factorization (e.g., $B' = U\Sigma$, $A' = V^\top$) creates a scale imbalance that destabilizes gradients after the switch. The symmetric split minimizes this:

\begin{theorem}[Optimal Conditioning]
\label{thm:condition}
Given the factorization $M = U \Sigma V^\top$, consider all rank-$r$ decompositions $M = B'A'$ where $B' = U\Sigma^\alpha$ and $A' = \Sigma^{1-\alpha} V^\top$ for $\alpha \in [0,1]$. The symmetric split ($\alpha = 1/2$) is the unique solution that minimizes the maximum condition number:
\begin{equation*}
    \min_{\alpha \in [0,1]} \max \left( \kappa(B'), \kappa(A') \right) = \sqrt{\kappa(M)}
\end{equation*}
\end{theorem}

Setting $\alpha=1/2$ equalizes the conditioning of both matrices (proof in Appendix~\ref{app:proofs}).

\paragraph{Transition stability.}
Two mechanisms ensure a smooth switch at $\Tswitch$:
\begin{itemize}
    \item \textbf{Loss invariance.} Eq.~\ref{eq:equivalence} guarantees $h(\Tswitch^-) = h(\Tswitch^+)$, so the loss passes through the boundary without discontinuity.
    \item \textbf{Momentum reset.} The optimizer's momentum and variance accumulators encode curvature from the sequential space, which is invalid after projection. We reset both to zero at $\Tswitch$~\citep{loshchilov2019adamw}.
\end{itemize}

\begin{algorithm}[t]
\caption{\jelly{}: Time-Axis Generalized Training via Task-Isomorphic Projection}
\label{alg:portal}
\begin{algorithmic}[1]
\REQUIRE Pretrained weight $\Wbase \in \mathbb{R}^{d_{out} \times d_{in}}$, rank $r$, switch step $\Tswitch$, total steps $T$
\STATE Initialize $A_{seq} \sim \mathcal{U}_{\text{Kaiming}}, \quad B_{seq} \sim \mathcal{N}(0, 10^{-4})$
\STATE Initialize optimizer states $m \gets \mathbf{0}, \quad v \gets \mathbf{0}$
\FOR{$t = 1$ \TO $T$}
    \IF{$t = \Tswitch$}
        \STATE $M \gets B_{seq} A_{seq} \Wbase$ \hfill $\triangleright$ Construct $M$
        \STATE $U, \Sigma, V^\top \gets \textsc{SVD}_r(M)$ \hfill $\triangleright$ SVD projection
        \STATE $A_{par} \gets \Sigma^{1/2} V^\top, \quad B_{par} \gets U\Sigma^{1/2}$ \hfill $\triangleright$ Energy splitting
        \STATE $m \gets \mathbf{0}, \quad v \gets \mathbf{0}$ \hfill $\triangleright$ Momentum reset
    \ENDIF
    
    \IF{$t < \Tswitch$}
        \STATE $h \gets (\mathbf{I} + B_{seq} A_{seq}) \Wbase x$ \hfill $\triangleright$ Sequential forward
    \ELSE
        \STATE $h \gets (\Wbase + B_{par} A_{par}) x$ \hfill $\triangleright$ Parallel forward
    \ENDIF
    
    \STATE $\mathcal{L} \gets \text{Loss}(h, y)$
    \STATE $(A, B), m, v \gets \text{Step}(\nabla_{\{A,B\}} \mathcal{L}, m, v)$ \hfill $\triangleright$ Update 
\ENDFOR
\STATE \textbf{return} $\Wbase + B_{par} A_{par}$ \hfill
\end{algorithmic}
\end{algorithm}

%=============================================================================
\section{Experiments}
\label{sec:experiments}
%=============================================================================

We evaluate \jelly{} on natural language understanding (NLU), reasoning, and generation tasks, comparing against state-of-the-art PEFT methods and analyzing task-manifold alignment via the $\Tswitch$ dynamics.

\subsection{Experimental Setup}

\paragraph{Models and Datasets.}
We evaluate on two model scales:
(1)~\textbf{DeBERTa-v3-base}~\citep{he2023debertav3} (86M) on seven GLUE~\citep{wang2019glue} benchmarks.
(2)~\textbf{LLaMA-2-7B}~\citep{touvron2023llama2} (7B) on mathematical reasoning (MetaMathQA~\citep{yu2024metamath} $\to$ GSM8K~\citep{cobbe2021gsm8k} and MATH~\citep{hendrycks2021math}), code generation (CodeFeedback~\citep{zhuo2024codefeedback} $\to$ HumanEval~\citep{chen2021humaneval} and MBPP~\citep{austin2021mbpp}), and conversation (WizardLM~\citep{xu2024wizardlm} $\to$ MT-Bench~\citep{zheng2023mtbench}).

\paragraph{Baselines and Implementation Details.}
Baselines: LoRA~\citep{hu2022lora} (Gaussian and Kaiming variants), PiSSA~\citep{meng2024pissa}, DoRA~\citep{liu2024dora}, AdaLoRA~\citep{zhang2023adalora}, and full fine-tuning.
All low-rank methods use rank $r = 8$ on identical target modules. For DeBERTa, adapters target query, key, and value projections. For LLaMA-2-7B, adapters target all seven linear projections ($W_q, W_k, W_v, W_o, W_{\mathrm{gate}}, W_{\mathrm{up}}, W_{\mathrm{down}}$), totaling $0.30\%$ trainable parameters.
Models are fine-tuned for 1 epoch on 100K subsampled training examples. We report \jelly{} with the optimal $\Tswitch$ per task category ($\Tswitch$ ablations in Section~\ref{sec:ablation}).

%-----------------------------------------------------------------------------
\subsection{Main Results}
\label{sec:main_results}
%-----------------------------------------------------------------------------
\begin{table}[t]
\caption{NLU evaluation on DeBERTa-v3-base across seven GLUE benchmarks. All adapter methods report the mean across 5 seeds with standard deviation ($\pm \sigma$) at rank $r = 8$. \jelly{} utilizes the empirically optimal $\Tswitch$ per task (see Table~\ref{tab:nlu_ablation}). Best results are in \textbf{bold}, second-best are \underline{underlined}. BitFit averages over 4 seeds due to one missing run.}
\label{tab:nlu}
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{llcccccccc}
\toprule
\textbf{Method} & \textbf{Params} & \textbf{SST-2} & \textbf{RTE} & \textbf{MNLI} & \textbf{MRPC} & \textbf{CoLA} & \textbf{QNLI} & \textbf{STS-B} & \textbf{Avg.} \\
\midrule
Full FT            & 184.4M (100\%) & 95.6 & \underline{84.8} & \underline{90.3} & \textbf{92.3} & \textbf{69.6} & 94.0 & \textbf{91.1} & \underline{88.2} \\
LoRA              & 0.44M (0.24\%) & 94.95 \scriptsize{$\pm$ 0.18} & 80.36 \scriptsize{$\pm$ 1.67} & 89.93 \scriptsize{$\pm$ 0.20} & 90.58 \scriptsize{$\pm$ 0.79} & 65.92 \scriptsize{$\pm$ 1.39} & 93.63 \scriptsize{$\pm$ 0.15} & 89.80 \scriptsize{$\pm$ 0.46} & 86.45 \\
LoRA (Gauss.)     & 0.44M (0.24\%) & 95.37 \scriptsize{$\pm$ 0.27} & 80.14 \scriptsize{$\pm$ 1.95} & 89.93 \scriptsize{$\pm$ 0.15} & 90.83 \scriptsize{$\pm$ 0.30} & 66.81 \scriptsize{$\pm$ 0.69} & 93.63 \scriptsize{$\pm$ 0.19} & 90.00 \scriptsize{$\pm$ 0.70} & 87.12 \\
LoRA (Kaiming)    & 0.44M (0.24\%) & 94.89 \scriptsize{$\pm$ 0.13} & 80.99 \scriptsize{$\pm$ 2.22} & 89.98 \scriptsize{$\pm$ 0.21} & 90.50 \scriptsize{$\pm$ 0.76} & 66.31 \scriptsize{$\pm$ 1.09} & 93.62 \scriptsize{$\pm$ 0.16} & 89.46 \scriptsize{$\pm$ 1.21} & 86.41 \\
PiSSA             & 0.44M (0.24\%) & 95.41 \scriptsize{$\pm$ 0.33} & 79.28 \scriptsize{$\pm$ 2.66} & 90.03 \scriptsize{$\pm$ 0.15} & 90.87 \scriptsize{$\pm$ 0.85} & 66.13 \scriptsize{$\pm$ 1.11} & 93.90 \scriptsize{$\pm$ 0.16} & \underline{90.57} \scriptsize{$\pm$ 0.28} & 86.60 \\
DoRA              & 0.47M (0.25\%) & 95.00 \scriptsize{$\pm$ 0.17} & 80.29 \scriptsize{$\pm$ 1.90} & 89.95 \scriptsize{$\pm$ 0.16} & 90.52 \scriptsize{$\pm$ 0.70} & 66.25 \scriptsize{$\pm$ 1.45} & 93.62 \scriptsize{$\pm$ 0.19} & 89.87 \scriptsize{$\pm$ 0.46} & 86.50 \\
AdaLoRA           & 0.44M (0.24\%) & 94.13 \scriptsize{$\pm$ 0.15} & 54.33 \scriptsize{$\pm$ 3.38} & 89.72 \scriptsize{$\pm$ 0.07} & 81.22 \scriptsize{$\pm$ 0.00} & 54.99 \scriptsize{$\pm$ 0.72} & 92.40 \scriptsize{$\pm$ 0.14} & 87.49 \scriptsize{$\pm$ 0.14} & 79.18 \\
BitFit            & 0.10M (0.05\%) & 94.93 \scriptsize{$\pm$ 0.11} & 75.90 \scriptsize{$\pm$ 0.74} & 89.62 \scriptsize{$\pm$ 0.08} & 89.72 \scriptsize{$\pm$ 0.23} & 63.54 \scriptsize{$\pm$ 0.78} & 93.02 \scriptsize{$\pm$ 0.03} & \underline{90.57} \scriptsize{$\pm$ 0.08} & 85.33 \\
\midrule
\textbf{\jelly{}} & 0.44M (0.24\%) & \textbf{95.71} \scriptsize{$\pm$ 0.22} & \textbf{81.68} \scriptsize{$\pm$ 2.42} & \textbf{90.23} \scriptsize{$\pm$ 0.07} & \underline{91.52} \scriptsize{$\pm$ 1.07} & \underline{67.08} \scriptsize{$\pm$ 0.54} & \textbf{93.94} \scriptsize{$\pm$ 0.14} & 90.19 \scriptsize{$\pm$ 0.40} & \textbf{87.19} \\
\bottomrule
\end{tabular}}
\end{table}

\begin{table}[t]
\caption{NLG evaluation across base models. \textbf{Math}: GSM8K, MATH accuracy (\%). \textbf{Code}: HumanEval, MBPP pass@1 (\%). \textbf{Conv.}: MT-Bench score. LLaMA-2-7B uses $r\!=\!8$ on all 7 linear projections (0.30\%); Mistral-7B and Gemma-7B use $r\!=\!128$ on $W_q, W_k, W_v, W_o$ (1.48\%). Best per model in \textbf{bold}, second-best \underline{underlined}.}
\label{tab:nlg_comprehensive}
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{ll cc cc c}
\toprule
& & \multicolumn{2}{c}{\textbf{Math}} & \multicolumn{2}{c}{\textbf{Code}} & \textbf{Conv.} \\
\cmidrule(lr){3-4} \cmidrule(lr){5-6} \cmidrule(lr){7-7}
\textbf{Model} & \textbf{Method} & \textbf{GSM8K} & \textbf{MATH} & \textbf{HumanEval} & \textbf{MBPP} & \textbf{MT-Bench} \\
\midrule
\multirow{3}{*}{LLaMA-2-7B}
& Full FT & -- & -- & -- & -- & -- \\
& LoRA & \textbf{53.10} & \underline{7.54} & \underline{25.0} & \underline{37.3} & -- \\
& \jelly{} (Ours) & \underline{51.30} & \textbf{8.18} & \textbf{25.6} & \textbf{38.9} & \textbf{6.22} \\
\midrule
\multirow{3}{*}{Mistral-7B}
& Full FT & -- & -- & -- & -- & -- \\
& LoRA & 67.48 & 16.68 & \textbf{47.6} & 56.1 & -- \\
& \jelly{} (Ours) & \textbf{69.45} & \textbf{18.24} & \textbf{47.6} & \textbf{60.6} & -- \\
\midrule
\multirow{3}{*}{Gemma-7B}
& Full FT & -- & -- & -- & -- & -- \\
& LoRA & \textbf{71.27} & \textbf{21.96} & 48.8 & 61.9 & \textbf{6.91} \\
& \jelly{} (Ours) & 69.52 & 20.80 & \textbf{51.8} & \textbf{62.7} & 6.89 \\
\bottomrule
\end{tabular}}
\end{table}
\textbf{NLU (Table~\ref{tab:nlu}).} \jelly{} achieves the highest average score (87.19) among PEFT methods, outperforming PiSSA (86.60), DoRA (86.50), and LoRA (86.45). Full fine-tuning leads on MRPC and CoLA with 100\% of parameters; \jelly{} attains competitive performance using only 0.24\% ($418\times$ fewer). The optimal $\Tswitch$ for NLU skews high (Table~\ref{tab:nlu_ablation}), confirming that NLU tasks rely on the semantic priors in $\Wbase$.

\textbf{NLG (Table~\ref{tab:nlg_comprehensive}).} \jelly{} achieves the highest MATH accuracy (8.18\%, +0.64 over LoRA) and MT-Bench score (6.22) on LLaMA-2-7B ($r\!=\!8$), while PiSSA leads on code generation (HumanEval 26.8, MBPP 39.9) via its SVD-based initialization that captures $\Wbase$'s code-relevant structure.
The divergence between GSM8K and MATH reflects task-manifold alignment: GSM8K favors LoRA's parallel exploration, while MATH benefits from \jelly{}'s sequential warm-up ($\Tswitch\!=\!0.8$).
At $r\!=\!128$, \jelly{} shows consistent code generation gains (Gemma: HumanEval $+3.0$, MBPP $+0.8$; Mistral: MBPP $+4.5$) and mathematical reasoning improvements on Mistral-7B (GSM8K $+1.97$, MATH $+1.56$), while LoRA retains the math advantage on Gemma-7B due to its stronger mathematical pretraining alignment. For Gemma-7B conversation quality, both methods score nearly identically (LoRA 6.91, \jelly{} 6.89).
Figure~\ref{fig:training_dynamics} visualizes the training dynamics.

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{figures/training_dynamics.pdf}
\caption{Training dynamics of LoRA and \jelly{} ($\Tswitch\!=\!0.2, 0.5, 0.8$) on MetaMath (LLaMA-2-7B, $r\!=\!128$). \textbf{(a)}~Training loss: \jelly{} converges faster during the sequential phase (inset: first 100 steps). \textbf{(b)}~Gradient norm: \jelly{}'s sequential warm-up produces more stable gradients. \textbf{(c)}~GSM8K accuracy evaluated every 1{,}000 steps.}
\label{fig:training_dynamics}
\end{figure}

%=============================================================================
% Analysis
%=============================================================================

%-----------------------------------------------------------------------------
\subsection{Task-Aware Subspace Analysis: $M$ vs.\ $\Wbase$ SVD}
\label{sec:subspace}
%-----------------------------------------------------------------------------

\jelly{}'s projection matrix $M = \Bseq \Aseq \Wbase$, constructed at $\Tswitch$ after sequential probing, encodes a task-aware subspace.
We measure how far $M$ deviates from $\Wbase$'s principal directions via the normalized Frobenius inner product of their top-$r$ singular vectors:
\begin{equation}
\mathrm{Sim}(U_W^{(r)}, U_M^{(r)}) = \frac{1}{r} \left\| {U_W^{(r)}}^\top U_M^{(r)} \right\|_F^2 \;\in\; [0, 1]
\label{eq:subspace_sim}
\end{equation}
A value of $1.0$ indicates identical spans; lower values indicate task-specific deviation.

\begin{table}[h!]
\caption{Subspace similarity between $\Wbase$ and \jelly{}'s task-aware matrix $M$ (layer-averaged). Lower values indicate greater deviation from $\Wbase$'s principal directions.}
\label{tab:subspace_similarity}
\centering
\small
\begin{tabular}{l ccc}
\toprule
\textbf{Task / Dataset} & \textbf{Left SV Sim ($U$)} & \textbf{Right SV Sim ($V$)} & \textbf{$\Delta\Sigma$ (\%)} \\
\midrule
MATH (Reasoning)      & 0.10 & 0.08 & -- \\
GSM8K (Arithmetic)    & 0.10 & 0.08 & -- \\
HumanEval (Code)      & 0.08 & 0.08 & -- \\
MT-Bench (Conv.)      & -- & -- & -- \\
SST-2 (NLU)           & 0.14 & 0.14 & -- \\
RTE (NLU)             & 0.03 & 0.03 & -- \\
MNLI (NLU)            & 0.10 & 0.10 & -- \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h!]
\centering
% Placeholder for subspace overlap heatmaps
\includegraphics[width=\linewidth]{figures/subspace_heatmap.pdf}
\caption{Subspace overlap between top-$r$ singular vectors of $\Wbase$ ($x$-axis) and $M$ ($y$-axis). \textbf{Left:} Identity baseline (no probing). \textbf{Center:} SST-2 retains near-diagonal structure. \textbf{Right:} MATH shows off-diagonal energy, indicating task-specific redirection.}
\label{fig:subspace_heatmap}
\end{figure}

As shown in Table~\ref{tab:subspace_similarity}, NLU tasks (SST-2, RTE) retain high overlap with $\Wbase$, confirming that pretrained features are directly useful---the sequential phase refines rather than redirects.
Tasks requiring novel capabilities (MATH, HumanEval) show lower similarity, indicating that the sequential warm-up steers $M$ away from $\Wbase$'s principal directions toward a task-intrinsic subspace (Figure~\ref{fig:subspace_heatmap}).
This confirms that the sequential phase performs data-driven subspace discovery rather than merely preserving $\Wbase$'s static structure.

%-----------------------------------------------------------------------------
\subsection{The $\Tswitch$ Spectrum: A Geometric Indicator of Task-Manifold Alignment}
\label{sec:ablation}
%-----------------------------------------------------------------------------

We show that $\Tswitch$ is not merely a hyperparameter but a geometric indicator of task-manifold alignment.

\begin{table}[h!]
\caption{$\Tswitch$ spectrum on MetaMath (LLaMA-2-7B, $r\!=\!8$). $\Tswitch = 0$ is standard LoRA.}
\label{tab:ablation}
\centering
\small
\begin{tabular}{lccc}
\toprule
$\Tswitch$ & \textbf{Loss} $\downarrow$ & \textbf{GSM8K} $\uparrow$ & \textbf{MATH} $\uparrow$ \\
\midrule
0.00 (LoRA) & 0.1974 & \textbf{53.1} & 7.54 \\
0.01 & 0.1962 & 50.9 & 7.54 \\
0.03 & 0.1957 & 51.1 & 7.96 \\
0.05 & 0.1958 & 51.5 & 7.40 \\
0.20 & 0.1957 & 51.6 & 8.12 \\
0.50 & 0.1953 & 51.6 & 7.52 \\
0.80 & \textbf{0.1948} & 51.3 & \textbf{8.18} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h!]
\caption{Cross-task $\Tswitch$ spectrum (normalized). Best per task in \textbf{bold}.}
\label{tab:tsw_spectrum}
\centering
\small
\resizebox{\linewidth}{!}{%
\begin{tabular}{ll cccc c}
\toprule
& & \multicolumn{4}{c}{\textbf{Switching Step ($\Tswitch$)}} & \\
\cmidrule(lr){3-6}
\textbf{Category} & \textbf{Task (Metric)} & \textbf{0.0 (LoRA)} & \textbf{0.2} & \textbf{0.5} & \textbf{1.0 (Seq)} & \textbf{Optimal $\Tswitch$} \\
\midrule
\multirow{2}{*}{Reasoning}
& GSM8K (Acc) & \textbf{53.10} & 51.60 & 51.60 & -- & 0.0 \\
& MATH (Acc)  & 7.54 & \textbf{8.12} & 7.52 & -- & 0.2 \\
\midrule
\multirow{2}{*}{Generation}
& HumanEval (pass@1) & 25.0 & 25.0 & \textbf{25.6} & 15.2 & 0.5 \\
& MT-Bench (Score)   & \textbf{6.22} & -- & \textbf{6.22} & 5.99 & 0.0 \\
\midrule
\multirow{4}{*}{NLU}
& SST-2 (Acc)  & 94.95 & 95.54 & \textbf{95.71} & 95.64 & 0.5 \\
& RTE (Acc)    & 80.36 & 80.22 & 80.52 & \textbf{82.31} & 1.0 \\
& MRPC (F1)    & 90.58 & 90.54 & 91.52 & \textbf{92.33} & 1.0 \\
& CoLA (MCC)   & 65.92 & 67.01 & 66.78 & \textbf{67.57} & 1.0 \\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure}[h!]
\centering
% Placeholder for task-manifold alignment curves
\includegraphics[width=0.85\linewidth]{figures/tsw_alignment_curves.pdf}
 \caption{Normalized performance vs.\ $\Tswitch$. \textcolor{red}{NLU tasks (red)} peak at $\Tswitch \to 1.0$; \textcolor{blue}{reasoning tasks (blue)} peak at intermediate values. The crossing point marks the boundary between knowledge-retention and knowledge-acquisition regimes.}
\label{fig:tsw_curves}
\end{figure}

Tables~\ref{tab:ablation} and \ref{tab:tsw_spectrum} reveal three patterns:

\paragraph{Monotonic loss improvement.}
Eval loss decreases from 0.1974 ($\Tswitch = 0$) to 0.1948 ($\Tswitch = 0.8$), a 1.3\% relative gain. Any sequential warm-up improves the optimization landscape.

\paragraph{Task-dependent optima.}
The optimal $\Tswitch$ varies by task category (Figure~\ref{fig:tsw_curves}):
\begin{itemize}
    \item \textbf{High alignment} (NLU): performance generally increases with $\Tswitch$; pure sequential mode ($\Tswitch = 1.0$) is optimal on most tasks (e.g., RTE, MRPC, CoLA), confirming the utility of pretrained features for classification.
    \item \textbf{Moderate alignment} (Code, Conversation): intermediate $\Tswitch$ balances manifold probing with parallel exploration. HumanEval peaks at $\Tswitch=0.5$ (25.6\%) and collapses to 15.2\% at $\Tswitch=1.0$, showing that pure sequential mode over-constrains the code subspace; MT-Bench is flat from $\Tswitch=0$ to $0.5$ (6.22) before declining at $\Tswitch=1.0$ (5.99), reflecting the lower structural novelty of conversational adaptation.
    \item \textbf{Low alignment} (Reasoning): a short probe suffices or LoRA ($\Tswitch=0$) is preferred; prolonged sequential coupling can impede task-specific subspace acquisition.
\end{itemize}

\paragraph{Micro-probe effect.}
Even $\Tswitch = 0.01$ (1\% of training) reduces eval loss by 0.6\%. A brief sequential interaction extracts the dominant task-relevant directions from $\Wbase$.

\paragraph{Geometric interpretation.}
$\Tswitch$ acts as a dial between knowledge retention (high $\Tswitch$) and knowledge acquisition (low $\Tswitch$), with the optimal value reflecting task-manifold alignment (Section~\ref{sec:subspace}).

%-----------------------------------------------------------------------------
\subsection{Quantization Experiment (Q-\jelly{})}
\label{sec:quantization}
%-----------------------------------------------------------------------------

Since the Task-Isomorphic Projection decomposes only the $r \times r$ core matrix, \jelly{} operates directly on 4-bit NF4-quantized weights without dequantizing $\Wbase$.

\begin{table}[h!]
\caption{Quantization error reduction ratio (\%) of QLoRA, LoftQ, PiSSA, and \jelly{} across attention layers ($Q, K, V, O$).}
\label{tab:quantization}
\centering
\small
\begin{tabular}{ll c cccc c}
\toprule
\textbf{Model} & \textbf{Method} & \textbf{Rank} & \textbf{Q} & \textbf{K} & \textbf{V} & \textbf{O} & \textbf{AVG} \\
\midrule
\multirow{4}{*}{LLaMA-2-7B}
& QLoRA & -- & 0 & 0 & 0 & 0 & 0 \\
& LoftQ & 128 & -- & -- & -- & -- & -- \\
& PiSSA & 128 & -- & -- & -- & -- & -- \\
& \textbf{\jelly{} (Ours)} & 128 & -- & -- & -- & -- & -- \\
\midrule
\multirow{4}{*}{LLaMA-3-8B}
& QLoRA & -- & 0 & 0 & 0 & 0 & 0 \\
& LoftQ & 128 & -- & -- & -- & -- & -- \\
& PiSSA & 128 & -- & -- & -- & -- & -- \\
& \textbf{\jelly{} (Ours)} & 128 & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}

\vspace{0.5em}

\centering
\includegraphics[width=0.9\linewidth]{figures/training_dynamics_quant.pdf}
\captionof{figure}{Training dynamics under 4-bit NF4 quantization on MetaMath (LLaMA-2-7B, $r\!=\!128$). \textbf{(a)}~Loss: Q-\jelly{} converges faster than QLoRA and LoftQ. \textbf{(b)}~Gradient norm: quantized sequential probing produces stable gradients. \textbf{(c)}~GSM8K accuracy at 1{,}000-step intervals.}
\label{fig:training_dynamics_quant}
\end{table}

%-----------------------------------------------------------------------------
\subsection{Initialization Cost}
\label{sec:init_cost}
%-----------------------------------------------------------------------------

\begin{table}[h!]
\caption{Initialization cost per linear layer ($r=8$).}
\label{tab:init_cost}
\centering
\small
\begin{tabular}{l ccc}
\toprule
\textbf{Model} & \textbf{$d$} & \textbf{PiSSA (FLOPs)} & \textbf{\jelly{} (FLOPs)} \\
\midrule
DeBERTa-base & 768 & $5.7 \times 10^{8}$ & $4.7 \times 10^{4}$ \\
LLaMA-2-7B  & 4{,}096  & $8.6 \times 10^{10}$ & $2.6 \times 10^{5}$ \\
LLaMA-2-70B & 8{,}192  & $6.9 \times 10^{11}$ & $5.2 \times 10^{5}$ \\
LLaMA-3-405B & 16{,}384 & $5.5 \times 10^{12}$ & $1.0 \times 10^{6}$ \\
\bottomrule
\end{tabular}
\end{table}

As shown in Table~\ref{tab:init_cost}, PiSSA requires $O(d^3)$ FLOPs per layer for full-weight SVD; \jelly{} requires $O(dr^2)$. At $d = 4096$ (LLaMA-2-7B), this is a $3.3 \times 10^5$-fold reduction.

%=============================================================================
\section{Conclusion}
\label{sec:conclusion}
%=============================================================================

We introduced \jelly{}, a framework that generalizes PEFT over the training time axis by dynamically transitioning adapter connectivity from sequential to parallel mode.
The Task-Isomorphic Projection losslessly converts sequential adapters into parallel adapters via rank-$r$ double SVD with $O(dr^2)$ cost.
Experiments demonstrate that \jelly{} consistently matches or outperforms LoRA, PiSSA, and DoRA across NLU, mathematical reasoning, code generation, and conversation.
The $\Tswitch$ spectrum provides a principled mechanism for balancing knowledge retention and acquisition, with the optimal value reflecting the intrinsic alignment between task and pretrained manifold.

\paragraph{Limitations.}
The optimal $\Tswitch$ requires validation-based selection; an adaptive mechanism based on gradient coherence remains future work.
Our experiments cover models up to 7B parameters; scaling behavior on larger models warrants investigation.

\paragraph{Broader impact.}
\jelly{} lowers the barrier to high-quality fine-tuning while maintaining LoRA's parameter and inference budget.

%=============================================================================
\bibliographystyle{plainnat}
\bibliography{references}

%=============================================================================
\appendix
%=============================================================================

\section{Proofs}
\label{app:proofs}

\subsection{Proof of Theorem~\ref{thm:condition}}
\begin{proof}
Let $M = U\Sigma V^\top$ with $\Sigma = \diag(\sigma_1, \ldots, \sigma_r)$, $\sigma_1 \geq \cdots \geq \sigma_r > 0$.
Since $U$ and $V$ are orthogonal matrices from the SVD, they do not affect the condition number: $\kappa(UX) = \kappa(X)$ for any orthogonal $U$.
For a factorization $B' = U\Sigma^\alpha$, $A' = \Sigma^{1-\alpha} V^\top$ with $\alpha \in [0,1]$, the condition number is determined solely by the diagonal $\Sigma^\alpha$:
\begin{align}
    \kappa(B') = \kappa(\Sigma^\alpha) = \kappa(M)^\alpha, \quad \kappa(A') = \kappa(\Sigma^{1-\alpha}) = \kappa(M)^{1-\alpha}.
\end{align}
The minimax problem $\min_\alpha \max(\kappa(B'), \kappa(A')) = \min_\alpha \kappa(M)^{\max(\alpha, 1-\alpha)}$ is solved at $\alpha = 1/2$, yielding $\kappa(B') = \kappa(A') = \sqrt{\kappa(M)}$.
\end{proof}

\subsection{Gradient flow analysis}

Let $\Wbase = U_W \Sigma_W V_W^\top$.
The sequential gradient $\nabla_A \mathcal{L}|_{\mathrm{seq}} \propto (\Wbase x)^\top = x^\top V_W \Sigma_W U_W^\top$ amplifies input components aligned with $\Wbase$'s right singular vectors, weighted by their singular values.
The adapter first adapts along directions where $\Wbase$ has the strongest signal.
The parallel gradient $\nabla_A \mathcal{L}|_{\mathrm{par}} \propto x^\top$ lacks this spectral bias and treats all directions equally.

\section{Extended experimental details}
\label{app:details}

\paragraph{LLaMA-2-7B.}
Learning rate $2 \times 10^{-4}$, batch size $4 \times 4$ (grad accum), cosine schedule, warmup ratio 0.03, BF16, sequence length 512.
Adapters applied to all 7 linear projections with $r = 8$, $\alpha = 8$, no dropout.

\paragraph{DeBERTa-v3-base.}
Learning rate $2 \times 10^{-4}$, batch size 32, linear warmup (10\%) + linear decay, FP32.
Adapters applied to query/key/value projections with $r = 8$, $\alpha = 8$.

\section{NLU $\Tswitch$ ablation}
\label{app:nlu_ablation}

\begin{table}[h]
\caption{$\Tswitch$ spectrum on GLUE (DeBERTa-v3-base). The optimal $\Tswitch$ varies by task complexity and data size, but late switching or pure sequential mode remains strong for classification tasks.}
\label{tab:nlu_ablation}
\centering
\small
\begin{tabular}{lcccccc}
\toprule
$\Tswitch$ & \textbf{SST-2} & \textbf{RTE} & \textbf{MRPC} & \textbf{CoLA} & \textbf{QNLI} & \textbf{STS-B} \\
\midrule
0 (LoRA) & 94.95 & 80.36 & 90.58 & 65.92 & 93.63 & 89.80 \\
1.0      & 95.14 & \textbf{81.68} & 90.87 & 66.82 & \textbf{93.94} & \textbf{90.19} \\
1.5      & 95.12 & 80.86 & 90.02 & \textbf{67.08} & 93.88 & 88.84 \\
3.0      & 95.54 & 80.22 & 90.54 & 67.01 & 93.90 & 89.32 \\
7.5      & \textbf{95.71} & 80.52 & \textbf{91.52} & 66.78 & 93.82 & 89.78 \\
\bottomrule
\end{tabular}
\end{table}

\section{Full Seed-wise NLU Results}
\label{app:full_seeds}

Table~\ref{tab:nlu_full_seeds} provides the complete results across 5 seeds for all NLU benchmarks. All methods use $r=8$ and identical hyperparameters ($lr=1e-4, bs=32$). \jelly{} results are reported using the optimal $\Tswitch$ per task.

\begin{table}[h]
\caption{Full NLU results across 5 seeds on DeBERTa-v3-base. BitFit results for Seed 42 are omitted due to execution failure; its average is computed over the remaining 4 seeds.}
\label{tab:nlu_full_seeds}
\centering
\scriptsize
\begin{tabular}{llcccccccc}
\toprule
\textbf{Seed} & \textbf{Method} & \textbf{SST-2} & \textbf{RTE} & \textbf{MNLI} & \textbf{MRPC} & \textbf{CoLA} & \textbf{QNLI} & \textbf{STS-B} & \textbf{Avg.} \\
\midrule
\multirow{6}{*}{16} 
& LoRA    & 95.07 & \textbf{79.78} & 89.76 & 89.82 & \underline{67.76} & 93.68 & 89.89 & 86.54 \\
& DoRA    & 95.18 & \underline{79.42} & \underline{89.85} & 89.86 & \textbf{67.99} & 93.63 & 89.93 & \underline{86.55} \\
& PiSSA   & \textbf{95.87} & 78.70 & 89.80 & \textbf{90.21} & 66.27 & \underline{93.89} & \underline{90.38} & 86.45 \\
& AdaLoRA & 93.92 & 50.54 & 89.64 & 81.22 & 54.84 & 92.24 & 87.53 & 78.56 \\
& BitFit  & 94.84 & 76.53 & 89.66 & \underline{89.93} & 63.68 & 92.99 & \textbf{90.47} & 85.44 \\
& \jelly{} & \underline{95.64} & \underline{79.42} & \textbf{90.07} & 89.44 & 67.08 & \textbf{94.03} & 90.34 & \textbf{86.58} \\
\midrule
\multirow{6}{*}{32} 
& LoRA    & 94.95 & \textbf{82.31} & \underline{90.16} & 90.20 & 65.80 & 93.76 & 90.11 & \underline{86.76} \\
& DoRA    & 94.95 & \underline{81.95} & 90.08 & 90.20 & 65.80 & 93.79 & 90.19 & 86.71 \\
& PiSSA   & \underline{95.64} & 78.70 & 89.96 & \underline{90.24} & \underline{66.02} & \underline{93.85} & \textbf{90.84} & 86.47 \\
& AdaLoRA & 94.04 & 55.60 & 89.71 & 81.22 & 54.79 & 92.29 & 87.39 & 79.29 \\
& BitFit  & 95.07 & 75.45 & 89.72 & 89.40 & 64.16 & 93.01 & \underline{90.66} & 85.35 \\
& \jelly{} & \textbf{95.99} & 81.23 & \textbf{90.24} & \textbf{91.61} & \textbf{67.03} & \textbf{93.96} & 89.51 & \textbf{87.08} \\
\midrule
\multirow{6}{*}{42} 
& LoRA    & 95.18 & 81.95 & 89.87 & \underline{91.01} & 64.36 & 93.61 & 89.00 & 86.43 \\
& DoRA    & 95.18 & \textbf{82.67} & 89.95 & 90.84 & 65.32 & 93.61 & 89.07 & 86.67 \\
& PiSSA   & \textbf{95.30} & \underline{82.31} & \underline{90.16} & 90.52 & \textbf{67.03} & \textbf{94.05} & \textbf{90.19} & \underline{87.08} \\
& AdaLoRA & 94.27 & 59.57 & 89.71 & 81.22 & 54.45 & 92.44 & 87.70 & 79.91 \\
& BitFit  & -- & -- & -- & -- & -- & -- & -- & -- \\
& \jelly{} & \textbf{95.30} & \underline{82.31} & \textbf{90.26} & \textbf{92.15} & \underline{66.28} & \underline{93.96} & \underline{90.12} & \textbf{87.20} \\
\midrule
\multirow{6}{*}{64} 
& LoRA    & 94.72 & 78.70 & \underline{90.13} & 90.13 & \underline{64.85} & 93.72 & 89.93 & 86.03 \\
& DoRA    & 94.84 & 78.34 & 90.12 & 90.13 & 64.61 & \underline{93.74} & 90.03 & 85.97 \\
& PiSSA   & \underline{95.07} & \underline{81.23} & 90.10 & \underline{91.10} & 64.32 & 93.67 & \underline{90.57} & \underline{86.58} \\
& AdaLoRA & 94.15 & 55.60 & 89.82 & 81.22 & 56.26 & 92.59 & 87.47 & 79.59 \\
& BitFit  & 94.84 & 76.53 & 89.55 & 89.72 & 62.41 & 93.06 & \textbf{90.60} & 85.25 \\
& \jelly{} & \textbf{95.76} & \textbf{85.20} & \textbf{90.25} & \textbf{91.46} & \textbf{67.99} & \textbf{93.96} & 90.39 & \textbf{87.86} \\
\midrule
\multirow{6}{*}{128} 
& LoRA    & 94.84 & \underline{79.06} & 89.74 & 91.76 & 66.80 & 93.37 & 90.07 & 86.52 \\
& DoRA    & 94.84 & \underline{79.06} & 89.75 & 91.59 & \textbf{67.53} & 93.30 & 90.12 & \underline{86.60} \\
& PiSSA   & \underline{95.18} & 75.45 & \underline{90.12} & \textbf{92.25} & 67.02 & \textbf{94.07} & \textbf{90.84} & 86.42 \\
& AdaLoRA & 94.27 & 50.90 & 89.75 & 81.22 & 54.61 & 92.46 & 87.35 & 78.65 \\
& BitFit  & 94.95 & 75.09 & 89.55 & 89.84 & 63.91 & 93.01 & 90.55 & 85.27 \\
& \jelly{} & \textbf{95.64} & \textbf{79.42} & \textbf{90.31} & \underline{92.23} & \underline{67.06} & \underline{93.67} & \underline{90.56} & \textbf{86.98} \\
\bottomrule
\end{tabular}
\end{table}

% Initialization cost table moved to Section~\ref{sec:init_cost} in the main text.

\section{Inference Latency}
\label{app:latency}

\begin{table}[h]
\caption{Inference latency (ms/token) and throughput (tokens/sec) on LLaMA-2-7B. \jelly{} merges into base weights at inference, incurring zero overhead.}
\label{tab:latency}
\centering
\small
\begin{tabular}{l ccc}
\toprule
\textbf{Method} & \textbf{Topology} & \textbf{Latency (ms/tok)} & \textbf{Throughput (tok/s)} \\
\midrule
Base Model      & None       & -- & -- \\
Houlsby & Sequential & -- & -- \\
LoRA            & Parallel   & -- & -- \\
\textbf{\jelly{}} & \textbf{Parallel (Merged)} & \textbf{--} & \textbf{--} \\
\bottomrule
\end{tabular}
\end{table}

After training, \jelly{}'s parallel adapters merge into $\Wbase + \Bpar\Apar$, producing a model structurally identical to LoRA. The latency and throughput match the base model exactly, unlike sequential adapters which add $O(dr)$ computation per layer.

\section{Injection Position and Rank Ablation}
\label{app:injection_rank}

\begin{table}[h]
\caption{Effect of adapter injection position on LLaMA-2-7B (MetaMath, $r=8$, $\Tswitch=0.8$).}
\label{tab:injection}
\centering
\small
\begin{tabular}{l cc cc}
\toprule
\textbf{Target Modules} & \textbf{Params} & \textbf{Eval Loss} & \textbf{GSM8K} & \textbf{MATH} \\
\midrule
$W_q, W_v$ & 0.06\% & -- & -- & -- \\
$W_q, W_k, W_v, W_o$ & 0.12\% & -- & -- & -- \\
All 7 linear & 0.30\% & -- & -- & -- \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\caption{Rank ablation on LLaMA-2-7B (MetaMath, $\Tswitch=0.8$, $W_q, W_k, W_v, W_o$).}
\label{tab:rank_ablation}
\centering
\small
\begin{tabular}{l cc cc}
\toprule
\textbf{Rank ($r$)} & \textbf{Params} & \textbf{Eval Loss} & \textbf{GSM8K} & \textbf{MATH} \\
\midrule
2 & 0.03\% & -- & -- & -- \\
4 & 0.06\% & -- & -- & -- \\
8 & 0.12\% & -- & -- & -- \\
16 & 0.24\% & -- & -- & -- \\
\bottomrule
\end{tabular}
\end{table}

\section{Full $\Tswitch$ Spectrum per Task}
\label{app:full_tswitch}

The NLU $\Tswitch$ ablation in Table~\ref{tab:nlu_ablation} demonstrates that late switching or pure sequential mode is consistently optimal for classification tasks. For NLU tasks where alignment with $\Wbase$ is highest, running the entire training in sequential mode achieves the performance upper bound. While sequential mode incurs inference latency proportional to model depth, it remains a valid strategy when accuracy is the primary objective. This observation suggests that $\Tswitch$ can serve not only as an optimization hyperparameter but also as a deployment-aware knob: practitioners can choose the point on the retention--acquisition spectrum that best matches their latency constraints.

%=============================================================================
\newpage
\input{checklist.tex}

\end{document}
